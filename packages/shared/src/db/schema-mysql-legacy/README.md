# MySQL schema (legacy)

Esquema Drizzle **MySQL v1** conservado solo como referencia histórica y para el script `scripts/migrate-mysql-to-libsql.ts`.

**Runtime y migraciones activas:** `packages/shared/src/db/sqlite/schema/` + `packages/shared/drizzle-sqlite/`.

No importar estos archivos en la app ni en `@raffle/shared/db`.
